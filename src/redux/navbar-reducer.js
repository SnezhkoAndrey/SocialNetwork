let initialState = {
  friends: [
    { id: 41, name: "Dima" },
    { id: 42, name: "Andrey" },
    { id: 43, name: "Sveta" },
  ],
};

const navbarReducer = (state = initialState, action) => {
  return state;
};

export default navbarReducer;
